/**
 * UI Constants for the Editor component and its sub-widgets.
 */

/** Delay in milliseconds before firing the onContentChange$ callback after typing stops */
export const DEBOUNCE_MS = 300;

/** Vertical offset in pixels for positioning the floating toolbar above the selection */
export const TOOLBAR_OFFSET_Y = 10;

/** Vertical offset in pixels for positioning the block type selector relative to the cursor */
export const SELECTOR_OFFSET_Y = 5;

/** Z-index for floating UI elements like toolbars and menus */
export const FLOATING_UI_Z_INDEX = 1000;

/** Default schema version for EditorDocument */
export const DOCUMENT_VERSION = "1.0";
